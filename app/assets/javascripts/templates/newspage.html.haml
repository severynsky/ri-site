%section.popup
  %a.btn.btn-info{"ng-href" => "#/news_items"}
    back
  .news-item-container
    .image
      %img(src="{{print.image}}")
    .text-container{"id" => "bind-html-with-trust"}
      %h3
        {{print.title}}
      %p{"ng-bind-html" => "deliberatelyTrustDangerousSnippet()"}
        -#{{print.body}}

%a.fadeout{"ng-href" => "#/news_items"}